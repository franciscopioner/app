import React from 'react';
import { LoaderStyled, LoaderContainer } from './styles';


const Loader: React.FC = () => {
  return (
    <LoaderContainer>
        <LoaderStyled />
    </LoaderContainer>
  )
};

export default Loader;
